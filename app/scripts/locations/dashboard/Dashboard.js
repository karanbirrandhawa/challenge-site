
var React = window.React = require('react');

// Instantiate site 
var Dashboard = React.createClass({
  render: function() {
    return (
      <div></div>
    );
  }
});

module.exports = Dashboard;